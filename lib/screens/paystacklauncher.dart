import 'package:flutter/material.dart';

class PayLauncher extends StatefulWidget {
  const PayLauncher({Key? key}) : super(key: key);

  @override
  _PayLauncherState createState() => _PayLauncherState();
}

class _PayLauncherState extends State<PayLauncher> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        body: Container(
          child: Column(

          ),
        ),
      ),
    );
  }
}
